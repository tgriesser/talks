var React = require('react');
var Slide = require('../lib/slide');

module.exports = React.createClass({

  render: function() {
    return (
      <div style={{position: 'relative', top: "200px"}}>
        <h1>Immutablity in JavaScript</h1>
      </div>
    );
  }

})